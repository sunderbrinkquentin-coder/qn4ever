import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
# Im VS-Terminal oder PowerShell:
git rm -r --cached .vs bin obj
git commit -m "Remove cached build directories"